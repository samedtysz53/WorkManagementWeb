<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Work Management</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f8f9fa; /* Light gray background */
            margin: 0;
            padding: 0;
            overflow-x: hidden;
        }

        .sidebar {
            height: 100vh;
            width: 250px;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #343a40; /* Dark gray sidebar */
            padding-top: 20px;
        }

            .sidebar a {
                padding: 10px;
                text-decoration: none;
                font-size: 18px;
                color: #ffffff; /* White text */
                display: block;
            }

        .button-row {
            margin-left: 260px;
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
        }

        .button {
            flex: 1;
            height: 150px;
            background-color: #6c757d; /* Gray button */
            color: #ffffff; /* White text */
            margin-right: 15px;
        }

        .frame {
            margin: 15px;
        }

            .frame button {
                display: block;
                width: 100%;
                margin: 10px 0;
                color: #ffffff; /* White text */
            }

        #media (max-width: 768px) {
            .sidebar

        {
            display: none;
        }

        .frame {
            margin-left: 0;
        }

        .button-row {
            margin-left: 0;
        }

        }

        .navbar {
            background-color: #343a40; /* Dark gray navbar */
        }

        .navbar-brand {
            color: #ffffff !important; /* White text for brand */
        }

        .navbar-toggler-icon {
            background-color: #ffffff; /* White color for toggler icon */
        }

        .navbar-nav .nav-link {
            color: #ffffff !important; /* White text for nav links */
        }

            .navbar-nav .nav-link:hover {
                color: #17a2b8 !important; /* Light blue on hover */
            }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark navbar-expand-md">
        <a class="navbar-brand" href="#">WorkManagement</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="navbar-nav">
                <a asp-controller="Main" asp-action="Day" class="nav-item nav-link">Günüm</a>

                <a asp-controller="Main" asp-action="CompletedTask" class="nav-item nav-link">Tamamlanan Görevler</a>
                <a asp-controller="Main" asp-action="worklist" class="nav-item nav-link">İş Listeleri</a>
                <a asp-controller="Main" asp-action="JobCreate" class="nav-item nav-link">İş Listesi Oluştur</a>
                <a asp-controller="Main" asp-action="SelectTeam" class="nav-item nav-link">Seçim</a>

                <a asp-controller="Main" asp-action="CloseD" class="nav-item nav-link">Çıkış yap</a>
            </div>
        </div>
    </nav>

    <div class="frame">
        @RenderBody()
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
